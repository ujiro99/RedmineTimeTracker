div#issue(ng-controller='IssueCtrl')

  form.form-inline(action='#', role='form')

    div

      // project
      div.form-group.project
        label.sr-only(for='selectProject') Project
        select#selectProject.form-control.input-sm(ng-model='selectedProject'
          , ng-options='project as project.name group by project.account.host for project in projects'
          , ng-change='onProjectChange()')

      // number
      div.form-group.issueNumber
        label.sr-only(for='inputIssueNumber') Ticket Number
        input#inputIssueNumber.form-control.input-sm(type='search', placeholder='search')

    // issues
    div.issueList
      ul
        li(ng-repeat='issue in issues')
          span.id {{issue.id}}
          span.subject {{issue.subject}}
          button.btn(type='button', ng-click='onClickIssueAdd(issue)', ng-hide='isAdded(issue)')
            i.icon-plus-sign
          button.btn(type='button', ng-click='onClickIssueRemove(issue)', ng-show='isAdded(issue)')
            i.icon-minus-sign

